#!/bin/bash
# Development server with Doppler secrets injection

set -e

cd "$(dirname "$0")/.."

echo "🔐 Loading secrets from Doppler..."
echo "🚀 Starting Phoenix server..."

doppler run -- mix phx.server
