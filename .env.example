# Local development environment variables
# Copy this to .env and fill in your values
#
# Option 1: Source manually before starting the server
#   source .env && mix phx.server
#
# Option 2: Use direnv (recommended)
#   brew install direnv
#   Add to ~/.zshrc: eval "$(direnv hook zsh)"
#   Run: direnv allow
#   Now .env loads automatically when you enter the directory

# =============================================================================
# Database Configuration
# =============================================================================
# Local development uses the default Postgres connection from config/dev.exs
# For production, set DATABASE_URL to your connection string
export DATABASE_URL=

# =============================================================================
# OpenRouter AI API
# =============================================================================
# Get API key from: https://openrouter.ai/keys
export OPENROUTER_API_KEY=

# =============================================================================
# Tigris Object Storage (S3-compatible)
# =============================================================================
# Used for database backups and file uploads
# Get credentials from: fly storage dashboard create
export TIGRIS_ACCESS_KEY_ID=
export TIGRIS_SECRET_ACCESS_KEY=
export TIGRIS_BUCKET_NAME=playground-dev

# =============================================================================
# Email Service (Resend)
# =============================================================================
# Get API key from: https://resend.com/api-keys
export RESEND_API_KEY=
export FROM_EMAIL=noreply@example.com

# =============================================================================
# Admin Seed User
# =============================================================================
# Credentials for the admin user created by seeds.exs
export ADMIN_EMAIL=admin@example.com
export ADMIN_PASSWORD=password123456

# =============================================================================
# Production Only (not needed for local development)
# =============================================================================
# export SECRET_KEY_BASE=
# export PHX_HOST=example.com
# export PORT=4000
